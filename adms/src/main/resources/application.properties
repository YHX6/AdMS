server.port=8090
# JDBC properties
spring.datasource.url=jdbc:mysql://localhost:3306/adms
spring.datasource.username=<t$z0<Zw(
spring.datasource.password=<t$z0<Zw(
#hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update

#loggin
logging.level.org.hibernate.sql=DEBUG
logging.level.com.jgchuanmei = ERROR
logging.level.web = DEBUG

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB
#add log file. THis is for linux system, and you have to create the fodler for log files ahead
logging.file.name=adms.log 
logging.file.path=/var/log/java-spring
